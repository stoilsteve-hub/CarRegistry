public class CarOwner extends Person {

    // Constructor for CarOwner
    public CarOwner(String name, String address, int age) {
        // Call the constructor of Person (the parent class)
        super(name, address, age);
    }
}
